<Window x:Class="JollyDiskPart.Dialogs.FormatPartitionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JollyDiskPart.Dialogs"
        mc:Ignorable="d"
        Title="Format Partition" Height="260" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="File System" Grid.Row="0"/>
        <ComboBox Grid.Row="1" Margin="0,5,0,10" SelectedValue="{Binding FileSystem}" SelectedValuePath="Content">
            <ComboBoxItem Content="NTFS"/>
            <ComboBoxItem Content="FAT32"/>
            <ComboBoxItem Content="FAT"/>
            <ComboBoxItem Content="exFAT"/>
        </ComboBox>

        <TextBlock Text="Label" Grid.Row="2"/>
        <TextBox Grid.Row="3" Margin="0,5,0,10" Text="{Binding Label}" />

        <StackPanel Grid.Row="4">
            <CheckBox Content="Quick Format" IsChecked="{Binding QuickFormat}" Margin="0,5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Format" Width="80" Margin="5" IsDefault="True" Click="Format_Click"/>
                <Button Content="Cancel" Width="80" Margin="5" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
