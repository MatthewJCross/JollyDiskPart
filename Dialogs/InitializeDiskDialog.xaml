<Window x:Class="JollyDiskPart.Dialogs.InitializeDiskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JollyDiskPart.Dialogs"
        mc:Ignorable="d"
        Title="InitializeDiskDialog" Height="220" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontWeight="Bold" FontSize="14" Text="Select partition style"/>
        <StackPanel Grid.Row="1" Margin="0,15,0,0">
            <RadioButton Content="GUID Partition Table (GPT)" IsChecked="{Binding UseGpt}" />
            <RadioButton Margin="0,10,0,0" Content="Master Boot Record (MBR)" IsChecked="{Binding UseMbr}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="5" Content="Initialize" IsDefault="True" Click="Ok_Click" Style="{StaticResource RoundButton}"/>
            <Button Width="80" Margin="5" Content="Cancel" IsCancel="True" Style="{StaticResource RoundButton}"/>
        </StackPanel>
    </Grid>
</Window>
