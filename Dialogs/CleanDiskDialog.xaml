<Window x:Class="JollyDiskPart.Dialogs.CleanDiskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JollyDiskPart.Dialogs"
        mc:Ignorable="d"
        Title="CleanDiskDialog" Height="240" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontWeight="Bold" FontSize="14" Text="This operation will erase the disk." />
        <CheckBox Grid.Row="1" Margin="0,15,0,10" Content="Perform Clean All (overwrite every sector)" IsChecked="{Binding CleanAll}" />
        <TextBlock Grid.Row="2" Text="{Binding Description}" TextWrapping="Wrap" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="5" Content="Clean" IsDefault="True" Click="Ok_Click"/>
            <Button Width="80" Margin="5" Content="Cancel" IsCancel="True"/>
        </StackPanel>    
    </Grid>
</Window>
