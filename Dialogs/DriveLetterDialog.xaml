<Window x:Class="JollyDiskPart.Dialogs.DriveLetterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JollyDiskPart.Dialogs"
        mc:Ignorable="d"
        Title="DriveLetterDialog" Height="180" Width="320" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Drive Letter:" Margin="0,0,0,8" FontWeight="SemiBold"/>
        <ComboBox Grid.Row="1" Width="120" HorizontalAlignment="Left" ItemsSource="{Binding AvailableLetters}" SelectedItem="{Binding SelectedLetter}" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" Text="Leave blank to remove the drive letter." Foreground="Gray" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="80" Margin="5,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Margin="5,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
