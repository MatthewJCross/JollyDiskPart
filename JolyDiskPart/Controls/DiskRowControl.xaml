<UserControl x:Class="JollyDiskPart.Controls.DiskRowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JollyDiskPart.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Left -->
        <StackPanel Margin="10">
            <TextBlock FontWeight="Bold" FontSize="24" Text="{Binding Name}"/>
            <TextBlock Text="{Binding Type}"/>
            <TextBlock Text="{Binding SizeGB, StringFormat={}{0:0.00} GB}"/>
            <TextBlock Text="{Binding Status}"/>
        </StackPanel>

        <!-- Right -->
        <ItemsControl Grid.Column="1" ItemsSource="{Binding Partitions}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="2" BorderBrush="#505050" BorderThickness="1" CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="8"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Border Background="{Binding Colour}"/>
                            <StackPanel Grid.Row="1" Margin="10">
                                <TextBlock FontWeight="Bold" Text="{Binding Name}"/>
                                <TextBlock Text="{Binding Size}"/>
                                <TextBlock Text="{Binding FileSystem}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
