<Window x:Class="JollyDiskPart.Views.ResizePartitionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JollyDiskPart.Views"
        xmlns:controls="clr-namespace:JollyDiskPart.Controls"
        mc:Ignorable="d"
        Title="Resize Partition" Height="420" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="18" Text="{Binding Partition.Label}"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="New Size MB"/>
            <Slider Minimum="{Binding MinimumSizeMB}" Maximum="{Binding MaximumSizeMB}" Value="{Binding NewSizeMB}" TickFrequency="1024" IsSnapToTickEnabled="False"/>
            <TextBlock Text="{Binding NewSizeMB,StringFormat={}{0:N0} MB}"/>
        </StackPanel>
        <controls:PartitionResizeBar Grid.Row="2" Height="60" DataContext="{Binding}"/>
        <!--<Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Margin="0,20,0,20">
            <ItemsControl ItemsSource="{Binding PreviewPartitions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="{Binding SizeMB}" Height="50" BorderBrush="Black" BorderThickness="1">
                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>-->
        <Button Grid.Row="3" Content="Apply" HorizontalAlignment="Right"/>
    </Grid>
</Window>
